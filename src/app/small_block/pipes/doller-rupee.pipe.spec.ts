import { DollerRupeePipe } from './doller-rupee.pipe';

describe('DollerRupeePipe', () => {
  it('create an instance', () => {
    const pipe = new DollerRupeePipe();
    expect(pipe).toBeTruthy();
  });
});
